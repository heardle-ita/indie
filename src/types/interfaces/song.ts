export interface SongConfig {
    trackName: string,
    others: string[],
    breaks: number[],
    artist?: string,
    day?: string,
    image?: string,
    song?: string,
    songLength: number,
    soundCloudLink?: string,
    soundSpotifyLink?: string
}